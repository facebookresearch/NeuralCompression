s4_total_2frame:
  save_dir: null # can put previous run here and it will be loaded

  module:
    num_pframes: 1
    training_stage: "4_total_2frame"
    learning_rate: 1e-4

  trainer:
    max_steps: 60000
